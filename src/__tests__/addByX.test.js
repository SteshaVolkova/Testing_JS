import { addByX } from "../addByX";

describe('addByx', () => {
    // .todo пометка, что тестов ещё нет, но нужно их написать
    it.todo('sould return a function');

    it.todo('should return a function which takes one argument and returns a sum with a value from original function');
});